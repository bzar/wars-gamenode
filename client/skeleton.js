function Skeleton(server) {
    this.server = server;
}

Skeleton.prototype.playerJoined = function(number, name) {
  
}

Skeleton.prototype.playerLeft = function(number) {
  
}

Skeleton.prototype.unitBanned = function(unitType) {
  
}

Skeleton.prototype.unitUnbanned = function(unitType) {
  
}

Skeleton.prototype.gameStarted = function(gameId) {
  
}

Skeleton.prototype.gameUpdate = function(gameId, tiles, turn, finished) {
  
}

Skeleton.prototype.tickerMessage = function(action, data) {
  
}

Skeleton.prototype.chatMessage = function(sender, time, message) {
  
}
